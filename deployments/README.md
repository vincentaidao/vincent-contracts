# Deployments

This folder is a simple, human- and machine-readable record of deployed contract addresses.

- `sepolia.json` (or `sepolia-latest.json`): latest Sepolia deployment addresses + tx hashes.
- Add other networks as needed.

Generated by deploy scripts in `scripts/`.
